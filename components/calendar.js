import React, {Component} from 'react';

export default class Calendar extends Component {
    render() {
        return (
            <div className="calendar">
                <button className="make-appointment">umów się online!</button>
            </div>
        )
    }
}
